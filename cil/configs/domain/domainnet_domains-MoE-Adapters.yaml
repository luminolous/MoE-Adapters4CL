hydra:
  run:
    dir: ./experiments/${scenario}/${dataset}_domains-${method}
  job:
    chdir: true

  job_logging:
    version: 1
    formatters:
      simple:
        format: '%(message)s'

class_order: ""
dataset_root: ""
workdir: ""
log_path: "metrics.json"

model_name: "ViT-B/16"
prompt_template: "a bad photo of a {}."

domainnet_txt_root: "txt"
domainnet_nb_classes: 345

batch_size: 32
initial_increment: 345
increment: 345
scenario: "domain"
dataset: "domainnet"

weight_decay: 0.0
l2: 0
ce_method: 0

method: "MoE-Adapters"
lr: 1e-3
ls: 0.0